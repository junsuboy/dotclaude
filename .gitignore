# Runtime state — 추적 안 함
cache/
sessions/
shell-snapshots/
file-history/
tasks/
telemetry/
statsig/
backups/
debug/
paste-cache/
session-env/

# Logs / history
history.jsonl
*.log

# Cache files
*-cache.json
mcp-needs-auth-cache.json
stats-cache.json

# Local override / sensitive
settings.local.json

# Plugin cache (외부 관리)
plugins/cache/

# Skill 자동 백업
skills/*/.backup/

# Agent runtime state
todos/

# Per-session memory (runtime, distinct from auto-memory)
projects/*/*/session-memory/

# OS / editor
.DS_Store
*.swp

# Session transcripts (회사 작업 대화록 — 절대 추적 금지)
projects/
